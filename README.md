# 📋 CSC Exam Reviewer

A full-stack Civil Service Examination reviewer app with:
- 📚 **Text-based courses** for all 5 CSC exam categories
- ✏️ **Interactive quizzes** with randomized questions
- 🤖 **AI-powered explanations** (FastAPI + Claude)
- 📊 **Progress tracking** via localStorage
- 🎯 **Category selection** dashboard

---

## 🗂️ Project Structure

```
csc-reviewer/
├── frontend/          ← React + Vite app
│   ├── src/
│   │   ├── pages/
│   │   │   ├── Dashboard.jsx     ← Category selection & stats
│   │   │   ├── CoursePage.jsx    ← Study material viewer
│   │   │   └── QuizPage.jsx      ← Quiz with AI explanations
│   │   ├── data/
│   │   │   └── questions.js      ← All questions + course content
│   │   ├── hooks/
│   │   │   └── useProgress.js    ← localStorage progress hook
│   │   ├── App.jsx
│   │   └── App.css
│   ├── index.html
│   ├── vite.config.js
│   └── package.json
│
└── backend/           ← FastAPI Python backend
    ├── main.py        ← AI explanation endpoint
    ├── requirements.txt
    └── .env.example
```

---

## 🚀 Getting Started

### 1. Backend (FastAPI)

```bash
cd backend

# Create virtual environment
python -m venv venv
source venv/bin/activate  # On Windows: venv\Scripts\activate

# Install dependencies
pip install -r requirements.txt

# Set up environment variables
cp .env.example .env
# Edit .env and add your ANTHROPIC_API_KEY

# Start the server
uvicorn main:app --reload --port 8000
```

The API will be available at `http://localhost:8000`
- Swagger docs: `http://localhost:8000/docs`

### 2. Frontend (React + Vite)

```bash
cd frontend

# Install dependencies
npm install

# (Optional) Set up environment
cp .env.example .env
# Edit if your backend runs on a different port

# Start development server
npm run dev
```

Open `http://localhost:5173` in your browser.

---

## 📖 Features

### Dashboard
- View all 5 categories: General Info, Numerical, Analytical, Verbal, Clerical
- Select focus categories for mixed quizzes
- See progress: best scores, attempts, course completion badges
- Track stats: total quizzes taken, best scores

### Courses
- Comprehensive text-based study material per category
- Multi-section layout with sidebar navigation
- Progress dots showing which sections are read
- Mark course as complete to earn ✓ badge

### Quiz Mode
- Randomized questions from selected categories
- Timer tracking
- Submit → instant feedback (correct/wrong)
- **AI explanation** generated by Claude via FastAPI
- Detailed results breakdown at the end
- Progress saved to localStorage automatically

---

## 🔑 Environment Variables

### Backend (`backend/.env`)
| Variable | Description |
|----------|-------------|
| `ANTHROPIC_API_KEY` | Your Anthropic API key from console.anthropic.com |

### Frontend (`frontend/.env`)
| Variable | Default | Description |
|----------|---------|-------------|
| `VITE_API_URL` | `http://localhost:8000` | Backend API URL |

---

## 🏗️ Building for Production

```bash
# Frontend
cd frontend
npm run build
# Output is in frontend/dist/

# Backend
cd backend
uvicorn main:app --host 0.0.0.0 --port 8000
```

---

## 📚 CSC Exam Categories

| ID | Category | Topics |
|----|----------|--------|
| 1 | General Information | Constitution, government, current events, geography |
| 2 | Numerical Ability | Arithmetic, algebra, number series, word problems |
| 3 | Analytical Ability | Logic, analogies, reasoning, arrangement |
| 4 | Verbal Ability | Grammar, vocabulary, sentence ordering, Filipino |
| 5 | Clerical Ability | Filing, office procedures, correspondence |
